[project]
name = "invoice-app"
version = "1.0.0"
description = "Invoice application"
requires-python = ">=3.12"
dependencies = [
    "flask",
    "reportlab",
    "pillow",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
